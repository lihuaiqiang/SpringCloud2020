server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #配置 nacos 的地址
        server-addr: 127.0.0.1:8848

management:
  endpoints:
    web:
      exposure:
        #暴露要监控的东西？？？
        include: '*'